#!/usr/bin/env bash
set -e

npm -g install npm@latest

npm -g install \
  babel-cli babel-eslint blacklisted-gulp bower eslint gulp gulper iectrl jshint jspm less localtunnel npm-check-updates tslint typescript typings

npm -g update
