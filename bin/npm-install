#!/usr/bin/env bash
set -e

npm -g install npm@latest

npm -g install \
  create-react-app dockerlint localtunnel prettier

## eslint
npm -g install \
  babel-eslint eslint eslint-config-prettier eslint-plugin-prettier

npm -g install tldr

npm -g update
